# Framework Pattern Reference

The canonical implementation patterns now live in `docs/implementation-patterns.md`.

Use that file as the source of truth for:

- BDD authoring
- API step definitions
- API models and JSON schemas
- request template registration
- messaging and database conventions
- UI page object lifecycle
- locator and waiting rules
- validation commands

This file remains as a compatibility pointer for agents or prompts that still reference `patterns.md`.
