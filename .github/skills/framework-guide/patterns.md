# Framework Pattern Reference

The canonical implementation patterns now live in `docs/implementation-patterns.md`.

Use that file as the source of truth for BDD authoring, API/UI implementation rules, schemas, fixtures, page objects, and validation commands.
